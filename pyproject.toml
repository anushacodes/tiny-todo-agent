[project]
name = "tiny-todo-agent"
version = "0.1.0"
description = "Tiny Todo Agent using Claude Agent SDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.2.152",
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
]

[tool.uv]
package = false
